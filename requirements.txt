# This file exists only to be empty for Vercel's Python framework detection.
# Local-dev deps for the broker/orchestrator live in requirements-local.txt.
# Vercel serverless functions use api/requirements.txt (also empty; stdlib only).
